import { Container,Typography,Button,Box,Stepper,Step,StepLabel} from '@mui/material';

const steps = ['Saisir vos informations', 'Confirmer'];

export default function ProcessCheckout() {

  return (
    <Container sx={{paddingTop:"30px"}}>
    <Box sx={{ width: '100%' }}>
     
    </Box>
    </Container>
  );
}